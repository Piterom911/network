import React from "react";
import s from './Login.module.css'

export const Login = () => {
    return <h1 className={s.title}>
        <span className={s.text}>Login to see the page</span>
    </h1>
}